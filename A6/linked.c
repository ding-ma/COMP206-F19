//
// Created by dingm on 2019-11-21.
//
#include <stdio.h>
#include "linked.h"

/**
 * create or update the linked list with the account number
 * @param account : account number of the user
 * @param amount : amount to be updated with
 */
void findUpdate(int account, float amount){ // arguments are passed in correctly
    printf("%d %f \n", account, amount);
    /*
     * with the 2 parameters, put it into the struct from linked.h
     * if account == exists, update the amount
     * else, create a new node
    */
}

/**
 * traverses through the linked list to print everything
 */
void prettyPrint(){

}